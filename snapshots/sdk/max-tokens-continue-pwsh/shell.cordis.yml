# Pin the PowerShell-facing tool set on every host with pwsh available.
- id: tool-bash
  disabled: true
- id: tool-pwsh
  disabled: false
- id: bash-sandbox
  disabled: true
- id: pwsh-sandbox
  disabled: false
# Keep this recorded header's optional tool roster; the scenario never calls Ralph.
- id: tool-ralph
  disabled: false
